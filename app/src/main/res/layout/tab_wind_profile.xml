<?xml version="1.0" encoding="utf-8"?>
<!--
    tab_wind_profile.xml — Tab 2: Multi-Altitude Wind Profile

    Fix: The previous layout had a LinearLayout root with the ScrollView getting
    layout_weight="1" / layout_height="0dp". This only works if the parent chain
    all have definite heights down to the root. Inside ATAK's TabHost the content
    view can be height-constrained in ways that collapse the weight-based child.

    Solution: Wrap the ENTIRE tab content in a NestedScrollView so ALL content
    scrolls as one unit. The WindChartView gets a fixed height (224dp) since it
    is a custom Canvas view that measures itself exactly. The text table gets
    wrap_content inside the scroll.
-->
<androidx.core.widget.NestedScrollView
  xmlns:android="http://schemas.android.com/apk/res/android"
  android:id="@+id/subTabWidget2"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:fillViewport="true">

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="8dp">

    <!-- ── Header row ─────────────────────────────────────────────────── -->
    <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:orientation="horizontal"
      android:gravity="center_vertical"
      android:layout_marginBottom="6dp">

      <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="@string/wind_profile_title"
        android:textSize="13sp"
        android:textStyle="bold"/>

      <Button
        android:id="@+id/wind_update_information_button"
        style="@style/darkButton"
        android:layout_width="wrap_content"
        android:layout_height="48dp"
        android:text="@string/wind_request_button"
        android:textSize="11sp"/>

    </LinearLayout>

    <!-- ── Wind Marker button ─────────────────────────────────────────── -->
    <Button
      android:id="@+id/btn_drop_wind_marker"
      style="@style/darkButton"
      android:layout_width="match_parent"
      android:layout_height="44dp"
      android:text="@string/wind_btn_drop_marker"
      android:textSize="11sp"
      android:layout_marginBottom="6dp"/>

    <!-- ── WindChartView — injected by WindProfileView ────────────────── -->
    <!--    Fixed height: 4 altitudes × 56dp = 224dp                      -->
    <FrameLayout
      android:id="@+id/wind_chart_frame"
      android:layout_width="match_parent"
      android:layout_height="224dp"
      android:background="@drawable/image_view_bg"
      android:layout_marginBottom="6dp"/>

    <!-- ── Hour scrubber ──────────────────────────────────────────────── -->
    <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:orientation="horizontal"
      android:gravity="center_vertical"
      android:layout_marginBottom="6dp">

      <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/wind_hour_label"
        android:textSize="10sp"
        android:layout_marginEnd="6dp"/>

      <SeekBar
        android:id="@+id/wind_seekbar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:max="167"/>

    </LinearLayout>

    <!-- ── Divider ───────────────────────────────────────────────────── -->
    <View
      android:layout_width="match_parent"
      android:layout_height="1dp"
      android:background="#33FFFFFF"
      android:layout_marginBottom="6dp"/>

    <!-- ── Monospaced text table (all rows, no inner scroll needed) ─── -->
    <TextView
      android:id="@+id/textview_tab3_waiting_json_data"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:fontFamily="monospace"
      android:textSize="11sp"
      android:padding="4dp"/>

  </LinearLayout>

</androidx.core.widget.NestedScrollView>
